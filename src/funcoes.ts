function calculaArea(base: number, altura: number): number{ //função de multiplicação
    return base * altura;
}

function saudacao (nome: string): string { //função de saudação
    return `Olá, ${nome}!`;
}
    